def divide(first, second):
    if second != 0:
        a = first / second
        return a
    elif second == 0:
        string_ = 'Ошибка'
        return string_


# result = divide(3, 0) # Проверка работы функции.
# print(result)
